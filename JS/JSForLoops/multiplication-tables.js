for (let firstNumber = 0; firstNumber < 11; firstNumber++) {
    console.log("table of " + firstNumber);
    for (let secondNumber = 0; secondNumber < 11; secondNumber++) {
        console.log(firstNumber + " * " + secondNumber + " = " + firstNumber*secondNumber);
    }
}